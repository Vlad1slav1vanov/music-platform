import { createTheme, Theme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5824f3',
    },
    secondary: {
      main: '#F13457',
    },
  },
})

export default theme;